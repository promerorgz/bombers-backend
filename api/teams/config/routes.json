{
  "routes": [
    {
      "method": "GET",
      "path": "/teams",
      "handler": "teams.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/teams/count",
      "handler": "teams.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/teams/:id",
      "handler": "teams.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/teams",
      "handler": "teams.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/teams/:id",
      "handler": "teams.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/teams/:id",
      "handler": "teams.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
